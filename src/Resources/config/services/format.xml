<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://symfony.com/schema/dic/services"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- Formats are auto-registered and explicitly tagged (no autowire/autoconfigure); the
             registry collects them via the setono_sylius_feed.format tag. The interface and the
             manually-wired registry are excluded. -->
        <prototype namespace="Setono\SyliusFeedPlugin\Format\"
                   resource="../../../Format/"
                   exclude="../../../Format/{FormatInterface.php,FormatRegistryInterface.php,FormatRegistry.php}">
            <tag name="setono_sylius_feed.format"/>
        </prototype>
    </services>
</container>
