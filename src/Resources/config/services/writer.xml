<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://symfony.com/schema/dic/services"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- Feed writers are auto-registered and explicitly tagged (no autowire/autoconfigure); the
             registry collects them via the setono_sylius_feed.writer tag. The interfaces, the
             manually-wired registry and the writer-config value object are excluded (an explicit tag
             applies to every prototyped class, so non-writers must be excluded). -->
        <prototype namespace="Setono\SyliusFeedPlugin\Writer\"
                   resource="../../../Writer/"
                   exclude="../../../Writer/{FeedWriterInterface.php,FeedWriterRegistryInterface.php,FeedWriterRegistry.php,WriterConfigInterface.php,XmlWriterConfig.php}">
            <tag name="setono_sylius_feed.writer"/>
        </prototype>
    </services>
</container>
