<?xml version="1.0" encoding="UTF-8" ?>

<container xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://symfony.com/schema/dic/services"
           xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <services>
        <!-- No autowire/autoconfigure: resolver dependencies are bound explicitly by name/type and
             the registry tag is applied explicitly below. -->
        <defaults>
            <bind key="$cacheManager" type="service" id="liip_imagine.cache.manager"/>
            <bind key="Setono\SyliusFeedPlugin\Currency\ContextCurrencyConverter"
                  type="service" id="Setono\SyliusFeedPlugin\Currency\ContextCurrencyConverter"/>
            <bind key="Symfony\Component\Routing\Generator\UrlGeneratorInterface"
                  type="service" id="router"/>
        </defaults>

        <!-- Value resolvers are auto-registered and explicitly tagged (setono_sylius_feed.value_resolver);
             the registry collects them via that tag. The interface and the manually-wired registry
             are excluded. -->
        <prototype namespace="Setono\SyliusFeedPlugin\ValueResolver\"
                   resource="../../../ValueResolver/"
                   exclude="../../../ValueResolver/{ValueResolverInterface.php,ValueResolverRegistryInterface.php,ValueResolverRegistry.php}">
            <tag name="setono_sylius_feed.value_resolver"/>
        </prototype>
    </services>
</container>
