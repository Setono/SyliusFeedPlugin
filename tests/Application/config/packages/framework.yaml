framework:
    csrf_protection: true
    form: true
    ide: phpstorm
    secret: '%env(APP_SECRET)%'
    session:
        handler_id: ~
    serializer:
        mapping:
            paths: [ '%kernel.project_dir%/config/serialization' ]
